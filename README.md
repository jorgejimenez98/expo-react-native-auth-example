# Expo React Native Auth Example

## Get started

1. Install dependencies

   ```bash
   pnpm install
   ```

2. Start the app

   ```bash
    pnpm start
   ```


### Notes
  * Run ``pnpm sort``` to sort all json files